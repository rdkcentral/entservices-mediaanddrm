set(callsign "org.rdk.GStreamerPlayer")
set(preconditions Platform)
set(startuporder ${PLUGIN_GSTREAMERPLAYER_STARTUPORDER})
set(autostart ${PLUGIN_GSTREAMERPLAYER_AUTOSTART})

map()
    key(root)
    map()
        kv(mode ${PLUGIN_GSTREAMERPLAYER_MODE})
    end()
end()

ans(configuration)
